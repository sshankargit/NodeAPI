You are generating Jest + Supertest tests for a Node.js Express API.
Return only complete JavaScript code. No markdown fences. No explanations.
Use: const request = require("supertest");
Use: const app = require("../src/server");
Use Jest describe/test/expect syntax.
Create positive, negative, contract, and business-rule tests.
Do not invent endpoints or expected values.
Prefer contract assertions: field exists, data type, allowed value, response status.
Only assert exact values if present in seedData.
created_at is a string in YYYY-MM-DD format, not a JavaScript Date object.

API specification:
{{API_SPEC_JSON}}
